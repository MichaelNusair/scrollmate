{
  "name": "scrollmate",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm nx run-many --target=build",
    "lint": "pnpm nx run-many --target=lint",
    "typecheck": "pnpm nx run-many --target=typecheck",
    "dev": "pnpm nx run web:dev",
    "prepare": "pnpm nx run shared:build"
  },
  "devDependencies": {
    "nx": "^20.4.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20"
  }
}
